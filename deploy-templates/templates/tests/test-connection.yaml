apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "node-js-boilerplate.fullname" . }}-test-connection"
  labels:
    {{- include "node-js-boilerplate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "node-js-boilerplate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
