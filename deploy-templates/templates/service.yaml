apiVersion: v1
kind: Service
metadata:
  name: {{ include "node-js-boilerplate.fullname" . }}
  labels:
    {{- include "node-js-boilerplate.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "node-js-boilerplate.selectorLabels" . | nindent 4 }}
